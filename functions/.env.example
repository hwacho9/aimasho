# Keep the default during local UI development and test runs.
USE_MOCK_MAPS=true

# Required only when USE_MOCK_MAPS=false. This is server-only; never expose it
# through Next.js public environment variables or Flutter app configuration.
GOOGLE_MAPS_SERVER_API_KEY=
